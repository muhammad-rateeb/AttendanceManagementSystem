@model List<AttendanceManagementSystem.Models.ViewModels.TeacherListViewModel>
@{
    ViewData["Title"] = "Manage Teachers";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-person-badge"></i> Manage Teachers</h2>
        <a asp-action="CreateTeacher" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add New Teacher
        </a>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>Employee ID</th>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Assigned Courses</th>
                            <th>Status</th>
                            <th>Created</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var teacher in Model)
                        {
                            <tr>
                                <td>@teacher.EmployeeId</td>
                                <td>@teacher.FullName</td>
                                <td>@teacher.Email</td>
                                <td><span class="badge bg-info">@teacher.AssignedCourses</span></td>
                                <td>
                                    @if (teacher.IsActive)
                                    {
                                        <span class="badge bg-success">Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Inactive</span>
                                    }
                                </td>
                                <td>@teacher.CreatedAt.ToString("MMM dd, yyyy")</td>
                                <td>
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="EditTeacher" asp-route-id="@teacher.Id" class="btn btn-outline-primary" title="Edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="ManageTeacherCourses" asp-route-id="@teacher.Id" class="btn btn-outline-info" title="Manage Courses">
                                            <i class="bi bi-book"></i>
                                        </a>
                                        <form asp-action="DeleteTeacher" method="post" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this teacher?');">
                                            <input type="hidden" name="id" value="@teacher.Id" />
                                            <button type="submit" class="btn btn-outline-danger" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            @if (!Model.Any())
            {
                <div class="text-center py-4">
                    <i class="bi bi-person-badge display-4 text-muted"></i>
                    <p class="text-muted mt-2">No teachers found.</p>
                </div>
            }
        </div>
    </div>
</div>
